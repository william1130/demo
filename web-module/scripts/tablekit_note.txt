若要更新TableKit版本, 請注意以下事項:

1.	因為原實作在排序時沒有考量rowSpan及colSpan的問題, 所以除了在widget.js中使用AdvancedTable來wrap部份function外, 必須修改TableKit.sort裡, 
    在處理header className的部份, 修改結果如下:
		var hcells = TableKit.getHeaderCells(null, cell);
		$A(hcells).each(function(c,i){
			c = $(c);
			c.removeClassName(op.ascendingClass);
			c.removeClassName(op.descendingClass);
			/** modified by Vincent, compare by object instead of index */
			//if( index === i )
			if(cell === c) {
				if(order === 1) {
					c.removeClassName(op.descendingClass);
					c.addClassName(op.ascendingClass);
				} else {
					c.removeClassName(op.ascendingClass);
					c.addClassName(op.descendingClass);
				}
			}
		});
